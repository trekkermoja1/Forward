bindPort = 7000

auth.method = "token"
auth.token = "{{ .Envs.FRP_TOKEN }}"

webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "{{ .Envs.DASHBOARD_USER }}"
webServer.password = "{{ .Envs.DASHBOARD_PASS }}"